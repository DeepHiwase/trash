import { NavigationBar } from "./NavigationBar";

export const Header = () => {
  return (
    <header>
      <h2>Header</h2>
      {/* <NavigationBar user={user} /> */}
      <NavigationBar />
    </header>
  );
};
