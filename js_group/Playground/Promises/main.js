// ============================= Promises ================================







// then only on first function if it returns promise or it will error -> after first promise -> you can put any number of .then .catch. .finally any where as all returns promise and even if one of them not return promise still works but first function needs to return promise